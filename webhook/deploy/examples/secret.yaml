# Replace TOKEN with an operator (or admin) API token minted in the coredns-ez UI
# (Tokens). Apply this before the ClusterIssuer can present challenges.
apiVersion: v1
kind: Secret
metadata:
  name: coredns-api-token
  namespace: cert-manager
type: Opaque
stringData:
  token: TOKEN
